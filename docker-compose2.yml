version: "3"
services:
  mosquitto:
    image: eclipse-mosquitto:1.6.8
    hostname: mosquitto
    container_name: mmosquitto
    expose:
      - "1933"
      - "9001"
    ports:
      - "11883:1933"
      - "9001:9001"
  zookeeper:
    image: 'bitnami/zookeeper:latest'
    container_name: mzookeeper
    ports:
      - '2181:2181'
    environment:
      - ALLOW_ANONYMOUS_LOGIN=yes
  kafka:
    image: 'bitnami/kafka:latest'
    container_name: mkafka
    ports:
      - '9092:9092'
    environment:
      - KAFKA_BROKER_ID=1
      - KAFKA_ZOOKEEPER_CONNECT=mzookeeper:2181
      - ALLOW_PLAINTEXT_LISTENER=yes
    depends_on:
      - zookeeper 
  kafdrop:
    image: 'obsidiandynamics/kafdrop:latest'
    depends_on:
      - kafka
    ports:
      - 9000:9000
    environment:
      - KAFKA_BROKERCONNECT=PLAINTEXT://kafka:9092
